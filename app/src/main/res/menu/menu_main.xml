<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".TimelineActivity">
    <item
        android:id="@+id/actionComposeButton"
        android:icon="@drawable/vector_compose_shortcut"
        app:showAsAction="ifRoom|withText"
        android:onClick="actionComposeButtonClicked"
        android:title="actionComposeButton" />

    <item
        android:id="@+id/miActionProgress"
        android:title="Loading..."
        android:visible="false"
        android:orderInCategory="100"
        app:showAsAction="always"
        app:actionLayout="@layout/action_view_progress" />
</menu>
